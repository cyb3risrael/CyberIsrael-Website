version: '3.9'

services:
  # ── Frontend (React + nginx) ──────────────────────────────
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: cyberisrael_frontend
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      # Uncomment for SSL certs:
      # - ./nginx/ssl:/etc/nginx/ssl:ro
    networks:
      - cyberisrael_net
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://localhost/health']
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      - 'traefik.enable=false'

  # ── Future: Node.js Backend ───────────────────────────────
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: cyberisrael_backend
  #   restart: unless-stopped
  #   ports:
  #     - '4000:4000'
  #   environment:
  #     - NODE_ENV=production
  #     - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/cyberisrael
  #     - JWT_SECRET=${JWT_SECRET}
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   networks:
  #     - cyberisrael_net

  # ── Future: PostgreSQL Database ───────────────────────────
  # db:
  #   image: postgres:16-alpine
  #   container_name: cyberisrael_db
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_DB: cyberisrael
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./backend/db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
  #   ports:
  #     - '5432:5432'
  #   networks:
  #     - cyberisrael_net
  #   healthcheck:
  #     test: ['CMD-SHELL', 'pg_isready -U postgres']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # ── Future: Redis Cache ───────────────────────────────────
  # redis:
  #   image: redis:7-alpine
  #   container_name: cyberisrael_redis
  #   restart: unless-stopped
  #   networks:
  #     - cyberisrael_net

networks:
  cyberisrael_net:
    driver: bridge

volumes:
  postgres_data:
